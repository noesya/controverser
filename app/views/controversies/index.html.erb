<% content_for :title, 'Un outil libre pour la cartographie de controverses' %>

<% @controversies.each do |controversy| %>
  <article class="controversy border-top py-4">
    <div class="row">
      <div class="col-lg-6">
        <h2>
          <%= link_to controversy, controversy, class: 'text-black' %>
        </h2>
      </div>
      <div class="col-lg-3 pt-2"><%= controversy.fragments.count %> fragments</div>
      <div class="col-lg-3 pt-2"><%= controversy.concepts.count %> concepts</div>
    </div>
  </article>
<% end %>
<article class="controversy border-top border-bottom py-4">
  <h2>
    <%= link_to "Cartographier une nouvelle controverse", new_controversy_path %>
  </h2>
</article>
